## 2026-05-21

- Updated `src/pages/PgaHub.tsx` and `src/pages/PgaBestBets.tsx` so `/pga` and `/pga/best-bets` route-level header/meta copy resolves from the current schedule entry first, with `FEATURED_PGA_TOURNAMENT` only as a fallback.
- Added stale-payload guarding for `public/data/pga/best-bets.json` so route headers do not surface an old tournament label when the feed lags behind the schedule.
- Verified `npm run build` passes after the PGA page updates.
- Important follow-up: `public/data/pga/best-bets.json` still contains `PGA Championship` / `Aronimink Golf Club` on 2026-05-21, while `src/data/pga/schedule.json` points to `THE CJ CUP Byron Nelson`. The UI shell now protects against that mismatch, but the feed itself still needs regeneration if the underlying picks content should match the current event.
