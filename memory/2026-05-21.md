## 2026-05-21

- Updated `src/pages/PgaHub.tsx` and `src/pages/PgaBestBets.tsx` so `/pga` and `/pga/best-bets` route-level header/meta copy resolves from the current schedule entry first, with `FEATURED_PGA_TOURNAMENT` only as a fallback.
- Added stale-payload guarding for `public/data/pga/best-bets.json` so route headers do not surface an old tournament label when the feed lags behind the schedule.
- Verified `npm run build` passes after the PGA page updates.
- Important follow-up: `public/data/pga/best-bets.json` still contains `PGA Championship` / `Aronimink Golf Club` on 2026-05-21, while `src/data/pga/schedule.json` points to `THE CJ CUP Byron Nelson`. The UI shell now protects against that mismatch, but the feed itself still needs regeneration if the underlying picks content should match the current event.
- Reworked the `/mlb` slate landing layout in `src/pages/MlbGameDetail.tsx` so the intro, sportsbook row, and prop-tool CTA stack in the primary column instead of sitting below a short hero block. This removes the large dead area that showed up beside the taller right-rail card on desktop.
- Updated `src/components/mlb/MlbMatchupLayout.tsx` to use the shared `site-page` / `site-container` shell tokens so the MLB landing page aligns with the rest of the site at every breakpoint.
- Restored `buildPitcherStrikeoutMatchupRows` in `src/pages/MlbHrProps.tsx` as a compatibility export for `useMlbPropsData`, mapping the newer strikeout row model back to the fields used by `/mlb`, `/mlb/props`, and `/mlb/strikeout-props`.
- Added runtime tournament-field filtering to `usePgaTournamentPlayers`: when a schedule entry has `workbook.fieldFile`, the tournament-specific PGA model now keeps only golfers listed in that field file, using the same player-name normalization/alias logic as the workbook export pipeline.
- Added `src/lib/pga/fieldFile.ts` plus focused tests so morning field refreshes can remove WD / non-field players from the weekly model without touching the ranking engine itself.
