## 2026-05-22

- Replaced `src/components/mlb/MlbMatchupHero.tsx` with the downloaded hero redesign: dark team-color gradient, ESPN team logos with fallback, MLB pitcher headshots with initials fallback, pitcher ERA/WHIP/K9 pills, and a new overall-edge tile under the hero.
- Replaced the `HomeSchedule` block inside `src/pages/MlbGameDetail.tsx` with the downloaded slate-card layout: ESPN logo treatment, gradient accent bars, status/filter cleanup, and a three-pill overall-edge row on each card.
- Added local slate helpers in `src/pages/MlbGameDetail.tsx` for ESPN logo URLs and fallback logo rendering, then removed the old now-unused slate preview helper/imports.
- Verified the MLB UI changes compile with `cmd /c npm run build`.
- Paused automated X posting by removing the `schedule` trigger from `.github/workflows/post-mlb-daily-picks.yml`; the workflow is now manual-only until the X setup is ready.
- Reworked the `/mlb` slate landing layout in `src/pages/MlbGameDetail.tsx` again so the daily prop preview is a persistent left rail on desktop, with the header, sportsbook bar, filters, and matchup card grid in the right column. Also gave `Top HR Props` and `Top K Props` distinct header colors/backgrounds for faster visual separation.
- Compacted the MLB matchup detail component set for higher information density across layout shells, hero, summary tiles, section cards/headers, comparison rows, lineup rows, prop angle cards, and supporting MLB pills/chips. Verified `cmd /c npm run build` still passes after the density pass.
