## 2026-05-23

- Refactored the `/mlb` Daily Prop Preview in `src/pages/MlbGameDetail.tsx` from a sticky narrow left rail into a compact full-width section. Added reusable `PropPreviewCard` rows for Top HR Props and Top K Props with table-style Player/Matchup/Score columns, subtle blue/green themes, tighter padding, aligned score pills, and responsive `md:grid-cols-2` behavior.
- Verified `npm run build` passes. `npm run lint` still fails on pre-existing repo lint errors in unrelated files plus existing `any` usage in `MlbGameDetail.tsx`; no new lint category was introduced by the prop preview refactor.
- Started local Vite dev server on `http://127.0.0.1:5173/mlb` and verified with headless Edge layout measurements: 1440/980/820 widths place HR and K cards on the same row; 390 width stacks them vertically.
- Implemented the downloaded Google Stitch MLB analytics hub redesign from `.codex-temp/stitch_mlb_analytics_hub_redesign` into the real `/mlb` page. Recreated the pale workspace, desktop MLB sidebar, navy hero, partner strip, compact prop cards, matchup analyzer, and six-card analytics tools grid while preserving live `useMlbPropsData` prop rows, MLB schedule/detail preview data, existing route links, and model logic. Did not copy Stitch placeholder players, stats, records, fake rankings, or fake matchup rows. Re-verified `npm run build` and headless Edge responsive checks at 1440/980/820/390 with no page-level horizontal overflow.
