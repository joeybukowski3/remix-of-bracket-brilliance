## 2026-05-24

- Refined the `/mlb` slate landing page in `src/pages/MlbGameDetail.tsx` without changing live data fetches, route behavior, or matchup/model logic.
- Widened the desktop home layout by increasing the left-rail/main-content grid widths and slightly expanding the hero/content card padding so the page uses more of the existing 1440px site container.
- Replaced the page-local slate/logo rendering with the shared `MlbTeamLogo` component for the `/mlb` prop preview rail and matchup analyzer cards, keeping abbreviation fallback behavior in-project instead of adding new assets or mock data.
- Updated the `Top HR Props` and `Top K Props` rail rows so both teams show compact logo + abbreviation pairs while keeping the score pill right-aligned and row height tight.
- Compacted the Game Matchup Analyzer cards into denser matchup modules: venue/time/status condensed up top, both teams rendered as compact identity blocks, starter rows moved into tight stat capsules, and the edge area converted into a smaller three-cell board with edge-team logos when the model names a side.
- Verified `cmd /c npm run build` passes.
- Verified `/mlb` locally by starting the Vite dev server and running headless Playwright smoke checks against `http://127.0.0.1:4173/mlb`; the page mounted with the live slate shell (`Today's MLB Slate`), runtime DOM contained rail and matchup logo images, and the 1440px + 390px viewport checks completed without page-level horizontal overflow after tightening the mobile rail constraints.
- `npm run lint` still fails repo-wide due pre-existing issues in unrelated files plus pre-existing `no-explicit-any` errors already present in `src/pages/MlbGameDetail.tsx`; this pass did not attempt lint cleanup outside the requested MLB UI work.

- Reorganized the MLB props experience around a shared `src/components/mlb/MlbPropModelComponents.tsx` presentation layer that consumes existing `useMlbPropsData()` rows and exported `MlbHrProps` row types without changing scoring formulas.
- Added a top-of-page `MLB Prop Model Dashboard` to `/mlb` with compact top-5 previews for HR props, strikeout props, and batter-vs-pitcher attackability, all using real model rows and MLB team logos.
- Rebuilt `/mlb/strikeout-props` and `/mlb/batter-vs-pitcher` as dedicated compact full-model pages with model headers, summary stats, top cards, search/team/game/confidence filters, sortable desktop tables, and mobile card layouts.
- Updated `/mlb/hr-props` metadata to say `MLB HR Prop Model`, converted the hero label to an `h1`, added internal links to the strikeout and batter-vs-pitcher model pages, and exported the existing row types for shared UI usage.
- Updated `scripts/generate-seo-files.mjs` so `/mlb/strikeout-props` and `/mlb/batter-vs-pitcher` stay in generated sitemaps.
- Verified `cmd /c npm run build` passes after the props redesign.
- Ran `npm run lint`; it still fails on existing repo-wide lint errors in unrelated files and pre-existing `any` usage in `MlbGameDetail.tsx`, but the new shared MLB props file no longer adds fast-refresh warnings after a local lint suppression.
- Ran headless Playwright checks on `/mlb`, `/mlb/hr-props`, `/mlb/strikeout-props`, and `/mlb/batter-vs-pitcher` at 1440px and 390px. All routes rendered real model content/logos and had no horizontal overflow. The only console issue observed was a report-only Google frame CSP warning on mobile `/mlb/hr-props`, likely from existing third-party/embed behavior.
