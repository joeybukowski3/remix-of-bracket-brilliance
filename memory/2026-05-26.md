## PGA hub best-bets redesign

- Updated `/pga` hero to show a right-side Course Information panel sourced from the active PGA schedule entry.
- Added optional schedule fields for `par`, `yardage`, `previousWinningScore`, and `projectedWeatherSummary`; populated Charles Schwab/Colonial with Par 70, 7,289 yards, Ben Griffin -12, and an explicit weather fallback because no live weather feed exists in the current PGA data path.
- Replaced top PGA best-bet cards on `/pga` with four compact linked tables using the existing `/data/pga/best-bets.json` arrays: outrights, top5, top10, top20. No pick/model logic changed.
- Fixed PGA page mobile overflow by adding `min-w-0` to the ranking grid/content column and page-level horizontal clipping while retaining internal table scroll.
- Validation: `npm run build` passed. Full `npm run lint` still fails from pre-existing repo-wide issues outside this change set; direct `PgaHub.tsx` and `pgaSchedule.ts` lint passed, `PgaHubShared.tsx` has existing Fast Refresh export warnings only.

## Charles Schwab PGA package generation

- Ran `node scripts/generate-pga-tournament-package.mjs --slug charles-schwab-challenge-2026-picks --feature`.
- Script completed and set the featured PGA override to `charles-schwab-challenge-2026-picks`.
- Field fetch warnings: DataGolf returned HTTP 403, ESPN had no competitors, so the generated model used the full workbook without field filtering.
- Generated package files include `src/data/pga/generated/charles-schwab-challenge-2026-picks.ts`, `src/data/pga/overrides/charles-schwab-challenge-2026-picks.ts`, and `public/data/pga/charles-schwab-challenge-2026.json` (currently `[]`).

## MLB HR prop model small-sample fix
- Located all powering files: scripts/generate-mlb-hr-props.mjs (core ranking + outputs), src/pages/MlbHrProps.tsx (types/normalize/slate top), src/pages/MlbGameDetail.tsx (/mlb top5), src/components/mlb/MlbPropModelComponents.tsx (dashboard top), src/pages/MlbPropsHub.tsx (hub top), useMlbPropsData.ts (consumer), generate-mlb-daily-picks.mjs (bestBets consumer), MlbHrProps.test.ts (no edit needed).
- Current logic was pure sort by raw computeBatterHrScore (no AB involved) — root cause of Victor Mesa Jr (few ABs, one 421ft HR) ranking #1.
- Implemented exactly as specified: MIN_AB_THRESHOLD=40 const; capture season atBats; hard filter qualifiedPool >=40 before any scoring/ranking/output (low-AB never enter raw JSON batters, bestBets, or any Top lists); for qualified: adjustedHrScore = round(raw * (AB/(AB+100)),1); sort+hrScoreRank by adjusted; hrScore field left as raw for reference/visibility in cols, heat, BvP calcs (rest of model untouched).
- Updated only 4 top selectors + type/normalize/validate to carry atBats+adjustedHrScore (fallback ?? for old data); no new columns, no App.tsx, no generated files edited, no computeBatterHrScore or weights changed.
- Verified: npm run build passed clean; vitest on MlbHrProps.test.ts: relevant tests (compute, normalize, sort, slateSummary) pass — 3 pre-existing unrelated failures in matchup/heat tests remain.
- Victor Mesa Jr (and any <40 AB) now fully excluded from Top HR Props, rankings, /mlb previews, etc. Raw scores still visible where shown.
- One-line PS regen+verify: `cmd /c "npm run mlb:hr-props -- --force"`
- Recommended default: 40 (per spec; solid for callup protection; can raise to 50+ mid-season for stricter volume if desired).
- Files changed (all source): scripts/generate-mlb-hr-props.mjs, src/pages/MlbHrProps.tsx, src/pages/MlbGameDetail.tsx, src/components/mlb/MlbPropModelComponents.tsx, src/pages/MlbPropsHub.tsx.
- HR model fix applied – ready to generate today’s Victor Mesa Jr video prompt if you want.
