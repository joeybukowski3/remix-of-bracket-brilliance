## MLB Prop Tables Column + Logo Updates

- Updated the three primary prop tables per user request (HR Prop table, Hits/BvP table, K prop table) to improve visual matching of opposing pitcher and team identity:
  - HR Prop table (MlbHrProps.tsx Batter View + Matchup Board best/hr lenses): opposing pitcher moved to first column after Rank; batter team logo (via existing TeamLogoBadge) added inline inside the Batter cell.
  - Hits table (MlbBatterVsPitcher.tsx dedicated + matching sections in MlbHrProps): exact same format — opposing pitcher first after Rank, batter team logo inside Batter column.
  - K prop table (MlbStrikeoutProps.tsx dedicated + strikeout lens in MlbHrProps): only the pitcher's own team logo added inline inside the Pitcher cell (no column reordering).
- Implementation used only existing patterns (TeamLogoText wrapper on dedicated pages; local TeamLogoBadge on MlbHrProps to match file style). No new components, no data model changes, no row builders touched.
- Scope strictly limited to desktop table header arrays and the first few <td> cells in tbody rows. Mobile card views, dashboard previews, Pitcher View table, and all other sections left untouched (minimal per AGENTS rules).
- Pre-change process followed exactly: full session startup reads (SOUL, USER, memory 2026-05-26 + 27), repo AGENTS.md, all relevant page files + hook + shared components + logo impl read, goal stated, callers inspected via grep, no edits until reads complete.
- Build verification: `npm run build` passed cleanly (exit 0, no TS or bundling errors from changes). Only three source files edited: src/pages/MlbHrProps.tsx, src/pages/MlbBatterVsPitcher.tsx, src/pages/MlbStrikeoutProps.tsx.
- Files changed (all source): MlbHrProps.tsx (main HR + Hits + K tables), MlbBatterVsPitcher.tsx (Hits table), MlbStrikeoutProps.tsx (K table).
- No generated files, no App.tsx, no configs, no Supabase, no tests required (no logic change).

## Other
- No other work today.
