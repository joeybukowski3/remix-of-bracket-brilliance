# 2026-06-02

## Live `/mlb` blank-page repair
- Live browser smoke test reproduced `ReferenceError: mlbOdds is not defined` only on `https://www.joeknowsball.com/mlb`.
- Root cause: the MLB odds integration defined `const mlbOdds = useMlbOdds()` in `MlbGameDetail`, but `HomeSchedule` referenced `mlbOdds` without receiving it.
- Fix: pass the existing `mlbOdds` value into `HomeSchedule` and declare its typed prop in `src/pages/MlbGameDetail.tsx`.
- Verification: `npm run build` passed. Production preview rendered `/`, `/mlb`, `/mlb/props`, `/mlb/hr-props`, `/mlb/strikeout-props`, `/pga`, and `/ncaa/bracket` with zero Playwright page exceptions.
- Existing baseline failures remain outside this targeted fix: `npm test` has five `src/pages/MlbHrProps.test.ts` expectation failures; `npm run lint` has repository-wide pre-existing issues including `src/lib/mlb/mlbPitcherRegression.ts` parse error.
