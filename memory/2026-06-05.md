# 2026-06-05

## MLB HR Props X Workflow
- Added a gated `--post-text-only` mode to `scripts/post-mlb-hr-props-to-x.mjs`.
- Text-only live mode requires `workflow_dispatch`, `X_ALLOW_LIVE_POST=true`, expected-account verification, production HR props data, and writes a duplicate receipt with `mode: "text-only"`.
- Text-only duplicate key appends `-text-only` so a live caption test does not block the normal media post duplicate key.
- Added safer X API error logging for status/code/title/detail while redacting known X secrets and Authorization-style values.
- Updated `.github/workflows/post-mlb-hr-props-to-x.yml` to expose `post-text-only` and wire duplicate cache/receipt handling for it.
- Verification: `node --check scripts/post-mlb-hr-props-to-x.mjs` passed.
- Verification: `npm run build` passed with existing large chunk and stale Browserslist warnings.
- Verification: `npm test` failed in existing `src/pages/MlbHrProps.test.ts` expectations unrelated to the X posting script/workflow edit.
