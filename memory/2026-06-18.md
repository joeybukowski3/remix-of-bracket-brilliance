# 2026-06-18

- Added MLB moneyline provider fallback work for `scripts/fetch-mlb-odds.mjs`.
- Kept The Odds API as primary for `baseball_mlb` h2h moneylines.
- Added server-side provider module `scripts/lib/mlb-moneyline-providers.mjs` with fallback order:
  1. The Odds API
  2. SportsGameOdds via `SPORTSGAMEODDS_API_KEY`
  3. Odds-API.io via `ODDS_API_IO_KEY`
  4. Empty `moneylines` object so existing visual no-odds fallback remains responsible for display.
- Preserved generated `moneylines[AWAY@HOME]` shape with `away/home.team`, `price`, `american`, and `implied`.
- Removed the old generated MLB Stats win-probability estimate path from the active moneyline flow because it was not a real odds provider and conflicted with the requested provider order.
- Added focused tests in `src/lib/mlb/mlbMoneylineProviders.test.ts`.
- Validation:
  - `node --check scripts/fetch-mlb-odds.mjs` passed.
  - `node --check scripts/lib/mlb-moneyline-providers.mjs` passed.
  - `npm test -- src/lib/mlb/mlbMoneylineProviders.test.ts` passed.
  - `npm run build` passed.
  - Full `npm test` still fails in unrelated existing `src/pages/MlbHrProps.test.ts` expectations.
