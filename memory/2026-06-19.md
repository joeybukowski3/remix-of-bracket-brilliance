# 2026-06-19

- Added the first NFL Super Bowl prediction-market tracker at `/nfl/super-bowl`.
- New server-side Polymarket Gamma endpoint: `api/nfl/super-bowl-odds.ts`.
  - Uses optional `POLYMARKET_SUPER_BOWL_EVENT_ID`.
  - Otherwise searches `public-search` for Super Bowl winner/champion events.
  - Requires an active, unresolved event with at least 28 recognizable NFL team markets.
  - Returns five-minute cache headers and stale warm-cache data if Polymarket subsequently fails.
- Added shared parsing/matching utilities in `src/lib/nfl/superBowlMarkets.ts`:
  - Parses stringified outcome arrays and selects the named Yes price.
  - Maps Polymarket aliases to the `NFL_POWER_RATINGS` canonical 32 teams.
  - Assigns dense market rank, calculates rank gap, and classifies signals.
- Added `NFLSuperBowlOdds.tsx`, route, and consistent three-item NFL subsection navigation on NFL, standings, and Super Bowl pages.
- Validation:
  - `npm test -- src/lib/nfl/superBowlMarkets.test.ts` passed (7 tests).
  - `npm run build` passed.
  - Full `npm test` still has 5 pre-existing unrelated failures in `src/pages/MlbHrProps.test.ts`; all NFL tests passed.
  - A live Polymarket public-search check on 2026-06-19 returned only closed historical Super Bowl events, so the new UI will correctly show its unavailable state until an active qualifying event is listed or the env override is set.

- Vercel preview validation:
  - Local `vercel dev` cannot render the Vite client with this repo's catch-all rewrite; it is a local Vercel/Vite port conflict, not an application route failure. Temporary `devCommand` and dynamic-port experiments were reverted.
  - Vercel serverless requires ESM-safe imports. Changed the NFL endpoint to import `superBowlMarkets.js` and the shared helper to import the NFL data module relatively with `.js`.
  - Preview API for `NFL Champion 2027` returned HTTP 200 JSON with all 32 canonical NFL teams and dense market ranks.
  - The preview deployment has Vercel Authentication enabled. Authenticated `vercel curl` verifies routes and the API, but normal browser visual/console checks require an SSO or protection-bypass browser session.
