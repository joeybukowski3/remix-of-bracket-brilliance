# 2026-07-08

- Created branch `mlb-numerology-buttondown-draft` from latest `origin/main`.
- Updated `.github/workflows/mlb-numerology-email.yml` so manual `mode=send` uses `npm run mlb:numerology:email:send` with `NUMEROLOGY_EMAIL_LIVE=true`, `NUMEROLOGY_EMAIL_WEBHOOK_URL` from secret or variable, and `NUMEROLOGY_EMAIL_WEBHOOK_TOKEN` from secret.
- Verified the receiver endpoint code creates Buttondown drafts only (`status: "draft"`), so the workflow wording now says draft creation instead of live email sending.
- Ran `npm run mlb:numerology:email:preview`; it generated local tracking artifacts. Restored tracked generated files and moved the untracked archive preview artifact to `C:\tmp\mlb-numerology-email-preview-2026-07-08.json`.
