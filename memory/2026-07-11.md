# 2026-07-11

## MLB X shared poll orchestrator (PR #67)
- Continued `feat/mlb-x-slate-timing` from clean head `c10572a`; no partial Claude efficiency-refactor files existed.
- Replaced the separate HR/K automatic 15-minute cron owners with `.github/workflows/poll-mlb-x-posts.yml`; the original HR/K workflows are manual-only and retain all dispatch inputs/safeguards.
- The lightweight plan checks both daily receipts before live data, builds one schedule/boxscore confirmation snapshot, and emits independent HR/K readiness decisions. Heavy setup/post jobs run separately only when ready.
- Shared poll concurrency is `poll-mlb-x-posts`; heavy jobs reuse the manual groups `post-mlb-hr-props-to-x` and `post-mlb-strikeout-props-to-x`, never cancel in-progress posts, and recheck receipts after acquiring those groups.
- Automatic `workflow_run` triggers were removed because the shared 15-minute cadence preserves bounded responsiveness without off-cadence duplicate runner starts.
- Posters still perform final live revalidation before producing immutable artifacts/posting, guarding against scratches between the poll snapshot and post.
- Validation passed: 21 new plan/gate/workflow tests; 117 focused PR #67 Vitest regressions; 43 Node tests including new, daily-lock, and DST cases; touched-file ESLint; Node syntax; YAML parsing; `git diff --check`; `npx tsc --noEmit`; `npm run build` (existing warnings only).
- Numerology and ML workflow hashes remained unchanged. No workflow dispatches or live X API calls were made.
