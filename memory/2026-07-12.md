# 2026-07-12

## MLB Numerology email renderer repair
- Confirmed the Cheerio mobile post-processor corrupted completed email HTML because `isSummaryTable()` used descendant `find("th")` matching and `mobileSummaryTable()` flattened descendant `find("tr")` rows into positional player fields.
- Removed the global post-processing helper and rendered mobile-safe header, top play, stacked summaries, detailed cards, tracking, and footer directly from structured play data.
- Email selection is now explicit as `emailSelectedPlays`: every distinct score above 65, filled in ranking order only until three when needed. Board, tracking, archive, scoring, and X-post lists remain unchanged.
- Added fail-closed HTML validation before webhook sends, focused selection/data-integrity tests, and real 375px/680px HTML plus PNG preview artifacts.
- Tracking is a marked standalone section immediately before the separately marked footer.
- No live email, workflow dispatch, push, deployment, scoring change, or historical-data edit was made.

## Tooling lesson
- Do not combine PowerShell verification or staging commands with semicolon separators; run each command separately to keep output clear and comply with repository tooling rules.
