# 2026-07-13

## PGA Top 20 Profile and Custom Model builder
- Created `codex/pga-model-presets-custom-builder` from `origin/main` SHA `6c96277c461cf1854048d15111d98a164a315590` in an isolated worktree so unrelated changes in the primary checkout remained untouched.
- Added the permanent runtime `Top 20 Profile` preset to the shared detailed PGA model architecture without editing generated tournament configs or player data.
- Top 20 weights: SG Approach 18, Par 4 10, Driving Accuracy 10, Bogey Avoidance 13, SG Around Green 8, JKB Trend 18, Birdie 125–150 4, SG Putting 7, Birdie under 125 2, Course True SG 10.
- Converted the existing always-visible sliders into a selectable, compact Custom Model with bounded number/slider controls, total warning, Normalize, Apply, Reset Default, and Load Top 20 actions.
- Custom weights use per-tournament versioned local storage, ignore unknown categories, fill newly registered categories from the active default, and reject corrupt, nonnumeric, or negative values.
- Fixed a model-config initialization race so embedded fallback presets are not persisted before the tournament's final JSON preset source is reconciled.
- Missing inputs retain the existing behavior: skip unavailable metrics, renormalize available weights, and require at least four/50% of active metrics. Existing presets and shared scoring logic were not changed.
- Focused suite passed 23/23. TypeScript, targeted ESLint, production build, and `git diff --check` passed. Browser validation passed on featured RBC Heritage desktop and tournament-specific U.S. Open mobile: preset/custom changes reordered players, reset restored defaults, rows stayed unique, and controls did not overflow.
- Full `npm test` was run once and had 8 unrelated baseline failures: one date-sensitive PGA sheet fixture plus seven existing MLB numerology contract/content/render assertions. No unrelated baseline files were changed.
- No external PGA provider call, odds integration, generated data edit, deployment, or merge occurred.
