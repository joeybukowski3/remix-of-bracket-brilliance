# 2026-07-15

## Homepage shared shell migration
- Created `fix/homepage-shared-header` from current `origin/main` in an isolated worktree because the primary checkout contained unrelated MLB and Supabase work.
- Migrated `HomeHeroOnly` from its custom inline header and direct footer to exactly one canonical `SiteShell`.
- Updated the sole shared-header navigation source to Home, MLB, NCAA, NFL (`/nfl/guide`), NBA, PGA, and The Open 2026; removed World Cup and Odds Tracker only from primary navigation.
- Kept the World Cup homepage card and confirmed `/world-cup` and `/odds-tracker` still render with HTTP 200 responses.
- Updated the homepage NFL card to `/nfl/guide` and the locked NBA status to `Coming Soon`.
- Production build, TypeScript, focused ESLint, and rendered checks at 390x844, 768x1024, and 1440x900 passed. Repository-wide lint and Vitest still have unrelated baseline failures.

## NCAA Football public gating
- Created `feat/gate-ncaa-football` on top of the unmerged homepage-shell commit so the next approved change retains the canonical shared header baseline.
- Replaced the public `/ncaa` tournament hub with the shared gated placeholder configured for NCAA Football, `Premium Access Coming Soon`, canonical `/ncaa`, and `noindex, follow`.
- Redirected `/rankings` to `/ncaa`; retained the schedule, matchup, betting-edge, bracket, detail, and team route implementations while noindexing every retired route shape.
- Removed retired NCAA links from the public header, homepage, and footer, and removed all NCAA URLs from sitemap generation without editing generated data.
- Shifted the shared header desktop breakpoint to `lg` after rendered review showed the longer NCAA Football label crowding the logo at 768px.
- TypeScript, focused ESLint, production build, sitemap syntax, and rendered checks at 390x844, 768x1024, and 1440x900 passed. Full Vitest reproduced nine unrelated baseline failures.

## MLB hub introduction
- Created `feat/mlb-hub-introduction` from merged `origin/main` at `ff1c1c53a00d139bfa72386a2822e2b0d9743e10` in an isolated worktree, preserving unrelated custom-model work in the primary checkout.
- Added the compact `/mlb` H1/intro, existing-data freshness row, model-edge heading and preview explanations, methodology callout, and expanded empty-slate copy without changing preview calculations, navigation, models, APIs, or generated data.
- Freshness uses the loaded schedule count, normalized `dashboard.date`, and normalized `dashboard.generatedAt`; the currently consumed normalized payload does not expose a valid `nextRunAt`, so that clause is omitted rather than fabricated.
- Updated only the `mlb` SEO registry title and description while preserving its canonical and robots behavior.
- TypeScript and production build passed. Browser checks passed at 390x844, 768x1024, and 1440x900 with one H1, stable layout, unchanged navigation/card links, and no overflow. Focused lint found only the existing legacy errors/warnings in `MlbGameDetail.tsx`; the other changed source files pass.

## MLB navigation consistency
- Created `fix/mlb-navigation-consistency` from `origin/main` at `c1da53c2409badd97b73628c884b5f23263a95cc` in an isolated worktree, preserving unrelated work in the primary checkout.
- Normalized the shared MLB sidebar/mobile menu into `Main` and `Models & Specials`, added visible Sin City and Numerology entries, and retained the existing anchor tools, partner block, glossary, and responsible-gambling text.
- Updated `MlbNavHero` to the approved seven destinations, removed its Numerology entry, and removed the floating mobile Numerology control; Numerology remains in the shared menu/sidebar and Analytics Tools grid.
- Completed the Analytics Tools grid with canonical labels and descriptions for HR Props, Strikeout Props, Batter vs Pitcher, Props Hub, Power Rankings, Sin City, and Numerology while retaining Today’s Slate and Model Edges.
- TypeScript, production build, focused navigation lint, and 26 focused Vitest assertions passed. Rendered checks at 390x844, 768x1024, and 1440x900 confirmed no overflow, the exact quick-nav order, canonical sidebar groups, and no Numerology FAB. `MlbGameDetail.tsx` still reports its pre-existing 28 `no-explicit-any` errors and two Fast Refresh warnings outside the edited lines.

## HR Props clarity and freshness
- Created `feat/hr-props-clarity` from `origin/main` at `7ee73c58d407a0b35173c5564a9fdca975070a86` in an isolated worktree.
- Added a truthful Eastern Time update label sourced only from normalized `dashboard.generatedAt`; `nextRunAt` is not retained by the current normalized HR dashboard payload and was omitted rather than inferred.
- Replaced the link-heavy introduction, removed the duplicate banner quick links, and added compact guidance for HR Score, sportsbook odds, raw market-implied percentage, the comparative table colors, and the pitcher-trend column.
- Added touch-accessible table-header popovers, an explicit missing-odds message, and compact links to the MLB Hub, Strikeout Props, Batter vs Pitcher, and Sin City without changing table data, sorting, filters, or either Best Bets surface.
- TypeScript, production build, and all 46 focused HR Props tests passed. Focused ESLint exactly matched the pre-existing file baseline. Browser checks at 390x844, 768x1024, and 1440x900 confirmed one H1, no document overflow, preserved table scrolling/sticky behavior, unchanged 18-batter rankings/scores/odds and Best Bets picks, and valid related-tool routes.

## Strikeout Props clarity
- Created `feat/strikeout-props-clarity` from `origin/main` at `68bf0ec0134a36aab35702b10c43d5f8eaf8ccc2` in an isolated worktree.
- Added compact page-use and always-visible Edge guidance, a Best Value explanation, accessible fixed-rank help, explicit missing-line/odds copy, and six related MLB tool links without changing freshness, calculations, sorting, filters, Best Bets, or Low Confidence behavior.
- Added a scoped `min-w-0` constraint to the existing table grid item after baseline validation exposed document-level overflow at 768px; the table retains its own horizontal scroller and sticky header.
- Captured pre/post parity with a controlled valid-market slate because the checked-in All-Star payload currently places both pitchers in Low Confidence. Rank order, K Scores, projections, lines, odds, Edge labels, Best Bets, search/confidence filtering, mobile cards, and row expansion matched exactly.
- TypeScript, production build, focused ESLint, and all 52 directly related Strikeout Props tests passed. Production-preview checks at 390x844, 768x1024, and 1440x900 confirmed one H1, no document overflow, preserved table/card behavior, and successful responses for all six related-tool routes.
