# 2026-07-23

## Vercel automation preview incident

- Paused unrelated K V2 UI work and created isolated branch `fix/vercel-automation-preview-builds` from latest `origin/main` (`39900009b44de9fa81529235b3f8903647f7deaa`).
- Investigated failed Vercel Preview commit `ad877b79f0df59b18233e23a5f6ed0bd07c82b0f` on `automation/mlb-x-state`.
- Confirmed the failed commit tree contains only 8 `mlb-x/.../diagnostics/*.json` files and does not contain `package.json`, `package-lock.json`, `vite.config.ts`, `src/`, `public/`, or `vercel.json`.
- Root cause: `automation/mlb-x-state` is an intentional orphan state-only branch; Vercel still tried to preview-build it, had no package metadata or repo `vercel.json`, and ran configured `vite build` without installed local Vite.
- Added state-store logic so future MLB X state commits carry a state-branch-only root `vercel.json` with an `ignoreCommand` that skips only `automation/mlb-x-state`.
- Added regression coverage proving first-time and legacy state branches include the Vercel ignore config while remaining state-only.
## MLB K projection V2 branch

- Created branch `NewKProp` from latest `origin/main` after `git fetch --prune origin`.
- Confirmed working tree was clean before branching.
- Confirmed `origin/main` SHA: `f55bd07fc486c46b08c25698a9edfcc980011008`.
- Verified PR #143 was merged on GitHub before branching; merge commit `db995f44a0271e90f8bb992dff70d125c7ddfbcc` is in current `origin/main` history.
- Phase 1 audit found current normal-starter public `Proj K` remains legacy in shadow mode: `projectedIP * projectedK9 / 9`.
- Added an audit artifact at `docs/mlb-k-projection-audit.md`.
- Added a standalone, non-wired V2 projection library at `src/lib/mlb/kProjectionV2.ts` with explicit fallback records and structured explanations.
- Added focused tests at `src/lib/mlb/kProjectionV2.test.ts`.
- Verification passed: `npm test -- src/lib/mlb/kProjectionV2.test.ts` and `npx eslint src/lib/mlb/kProjectionV2.ts src/lib/mlb/kProjectionV2.test.ts`.
- Expanded V2 tests to 22 cases covering workload fallback order, venue and handedness splits, missing metric renormalization, invalid input sanitization, no NaN/Infinity, deterministic output, no sportsbook-line input, and component reconciliation.

## MLB K props V2 shadow artifact

- Added server-side canonical shadow artifact generation for `public/data/mlb/k-props-v2-shadow.json`.
- Generator entrypoint: `scripts/generate-mlb-k-props-v2-shadow.mjs`.
- Testable artifact core: `scripts/lib/mlb-k-props-v2-shadow-core.mjs`.
- Package script: `npm run mlb:k-props-v2-shadow`.
- Artifact rows persist legacy projection, V2 projection, comparison edges, normalized V2 inputs, workload joins, lineup summaries, detail joins, availability flags, fallbacks, warnings, and diagnostics.
- Fixed V2 recent-regression behavior so recent components are included only when an observed recent signal exists.
- Current local artifact generation produced 10 rows with 10 V2-computed rows and no missing workload/opponent/lineup rows.
- Verification passed: `npm test -- src/lib/mlb/kProjectionV2.test.ts src/lib/mlb/kPropsV2ShadowArtifact.test.ts`, `node --check` for both new scripts, and ESLint for new TS/script files.
- Committed and pushed this phase at `6563d0a8a568da968d570190c9a8fb5393c94c86`.
- Added explicit artifact validation in `scripts/lib/mlb-k-props-v2-shadow-validator.mjs` plus CLI `scripts/validate-mlb-k-props-v2-shadow.mjs`.
- Generator now validates the full artifact before atomic temp-file publication.
- Validator checks schema version, shadow mode, model/date fields, deterministic row order, unique identities, finite projections, null unavailable inputs, retained legacy/V2 projections, sportsbook exclusion from V2 inputs, and diagnostic reconciliation.
- Continued phase from `967ac58f29a23cd2f09a4a37a65bba26833eda04`: added exact npm validation, source-date integrity, innings/recent helpers, daily workflow integration, comparison/backtest harness, and split-source feasibility documentation.
- Continued UI phase from `b188a8240e52f375db14b1dfe8e8b64faeac4f11`.
- Added internal-only K Projection V2 shadow UI behind `/mlb/strikeout-props?debug=k-v2`; normal public mode does not fetch or show V2 metrics.
- Added `useMlbKPropsV2Shadow` with defensive top-level artifact validation, current-slate enforcement, stable `gameId|pitcherId` matching, strict unambiguous fallback matching, and fail-closed missing/stale/invalid behavior.
- Public `Proj K` remains the legacy `row.projectedKs`; V2 comparison is separate, labeled Shadow/Experimental/not production/not historically validated, and does not claim accuracy.
- Expanded K detail panels now render recent performance with canonical shadow AVG summaries when available, plus debug-only projection comparison, V2 component breakdown, split availability, and source integrity panels.
- Browser validation with local preview confirmed no document-level horizontal overflow at 1440, 1280, 1024, 768, or 390 widths in both normal and debug modes; missing and stale shadow artifact simulations preserve legacy rows and suppress V2 comparisons.
- Added reproducible browser audit script `scripts/audit/mlb-k-v2-shadow-ui-browser-check.mjs` and documented measurements in `docs/mlb-k-v2-shadow-ui-validation.md`.

## MLB K pitcher detail data layer

- On `feat/mlb-k-pitcher-detail-splits`, restored main-compatible detail fetch/core fallbacks while retaining enriched pitcher starts and venue splits.
- Verified live StatsAPI game-log paths, fixed `gameId` → `gamePk`, added both stable-ID match keys, and made legacy matching unambiguous-only.
- Reused `scripts/lib/mlb-baseball-innings.mjs` instead of importing a TypeScript helper from Node `.mjs`.
- Added compact fixtures/tests for site, hits, pitch counts, innings/outs, H/9, K/9, venue totals, site-specific recent starts, same-day exclusion, and duplicate removal.
- Regenerated `public/data/mlb/strikeout-prop-details.json`; audit passed for 10 records and 39 complete recent starts with no API, stale, duplicate, or stable-key failures.
