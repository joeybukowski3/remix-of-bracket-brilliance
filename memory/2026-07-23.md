# 2026-07-23

## Vercel automation preview incident

- Paused unrelated K V2 UI work and created isolated branch `fix/vercel-automation-preview-builds` from latest `origin/main` (`39900009b44de9fa81529235b3f8903647f7deaa`).
- Investigated failed Vercel Preview commit `ad877b79f0df59b18233e23a5f6ed0bd07c82b0f` on `automation/mlb-x-state`.
- Confirmed the failed commit tree contains only 8 `mlb-x/.../diagnostics/*.json` files and does not contain `package.json`, `package-lock.json`, `vite.config.ts`, `src/`, `public/`, or `vercel.json`.
- Root cause: `automation/mlb-x-state` is an intentional orphan state-only branch; Vercel still tried to preview-build it, had no package metadata or repo `vercel.json`, and ran configured `vite build` without installed local Vite.
- Added state-store logic so future MLB X state commits carry a state-branch-only root `vercel.json` with an `ignoreCommand` that skips only `automation/mlb-x-state`.
- Added regression coverage proving first-time and legacy state branches include the Vercel ignore config while remaining state-only.
