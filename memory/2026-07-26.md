# 2026-07-26

## Support the Site page

- Created `feat/support-the-site-page` from `origin/main` at `cd46dc82c0d1dd1849d7d16c0e62cccf88fdb15f`.
- Added the public `/support` page with shared shell, concise support context, optional early-supporter benefits, conditional Stripe and Bitcoin support, and `/support` SEO metadata.
- Stripe uses only `VITE_STRIPE_SUPPORT_URL`; missing or invalid values render a disabled non-link action.
- Bitcoin uses only `VITE_BITCOIN_SUPPORT_ADDRESS`; the entire block stays hidden when missing, wraps safely when configured, and reports clipboard success or failure.
- Added `Support the Site` to shared desktop/mobile navigation, moved the footer link to `/support`, and made legacy `/donate` redirect to `/support` so the previous hardcoded Stripe URL is no longer reachable.
- Focused Support/App/header tests passed (19 tests). Scoped ESLint, `tsc --noEmit`, production build, and `git diff --check` passed.
- Local Chromium checks at 1440px, 1024px, and 390px passed for configured and missing payment states with zero horizontal overflow.
- No secret keys, payment form, payment data collection, merge, or deployment were added.
