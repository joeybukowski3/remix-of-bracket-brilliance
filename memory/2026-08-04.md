# 2026-08-04

- Audited `/pga` and `/pga/dfs` on `fix/pga-dfs-ranking-sync-and-player-details`, updated to `origin/main` commit `78e336cacb0d0ea97367be32cd7275c9f33364ef`.
- Root cause: `/pga` used the newer history-aware tournament model and displayed official-field rank, while DFS independently used legacy default/course-weight formulas over all 166 stat rows. Cameron Young was an exact identity match: canonical field rank #1, legacy DFS model rank #6, and legacy all-player tournament rank #8.
- Extracted the current PGA model row builder for shared use by `/pga` and DFS; current tournament/custom comparison ranks now use the validated official-field player universe.
- Added shared current-field loading/validation and bidirectional player aliases so official field names and PGA player IDs resolve to canonical model identities.
- DFS comparison records now carry canonical identity, nationality, official ID, model row/components, current and tour ranks, recent starts, tournament history, and JKB trend. Expanded row details expose that data.
- Fixed the comparison selector to control value sorting/filtering. Value remains `salaryRank - comparisonRank`, so positive means underpriced and negative means overpriced.
- Verified current Wyndham data: 166 stat rows, 141 official entrants, 124 modeled entrants; Cameron Young resolves to canonical field #1 and field-scoped tournament #1. Focused tests passed 21/21, TypeScript passed, production build passed with only existing NFL duplicate-key/chunk-size/Browserslist warnings.
- Final acceptance audit added explicit `FULL_MODEL` / `PARTIAL` / `SALARY_BASELINE` coverage. Salary-only and partial golfers stay visible with real salary/salary rank, nullable unavailable ranks/values, and cannot pass Value Plays Only on a missing selected metric. Removed the all-tour Model Rank fallback for golfers outside the validated current field.
- Centralized deterministic table sorting/filtering and added rendered UI coverage for search/clear, salary bounds, selected comparison mode, the +3 Value Plays threshold, all requested sortable headers, and correct expandable player details. Missing detail values now render `—`.
- Cross-checked ten modeled Wyndham golfers: `/pga` field rank and DFS Model Rank matched for Cameron Young, Hideki Matsuyama, Ryan Gerard, Patrick Cantlay, Maverick McNealy, Sungjae Im, Keegan Bradley, David Lipsky, Ryo Hisatsune, and Austin Smotherman.
- Final validation: `git diff --check`, `npx tsc --noEmit`, 58 focused tests across 8 files, and `npm run build` passed. Branch remains uncommitted.
