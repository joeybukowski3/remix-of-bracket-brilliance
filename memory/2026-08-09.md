# 2026-08-09

## College Football Phase 2B integration boundary

- Branch remains `feat/cfb-phase-1-foundation`, HEAD `5476aad0`; no commit/push/deploy.
- `CFBD_API_KEY` was unavailable in the process and repo env files, so no CFBD data was fetched,
  no real ratings were generated, and sample UI data was intentionally left active.
- Added CFBD v2 bulk-fetch/cache architecture (teams, 2025 games and team-game stats, 2026
  schedule, optional returning production and talent) with atomic writes and SHA-256 manifest.
- Added centralized 138-team CFBD name/JKB ID/slug/ESPN ID mapping and alias normalization.
- Added normalized historical games/performance, explicit postseason categories, FCS policy, and
  deterministic 12-iteration opponent adjustment on offense/defense yards per play.
- Added rating/SOS generation and validation script targeting `data/generated/cfb/`, but it
  truthfully fails before output until authenticated raw caches exist.
- Current CFBD returning-production schema is offense-only; defense and QB remain null. Talent is
  retained but Phase 2A's zero talent weight is unchanged.
- Verification: 8 new Phase 2B tests passed; full affected CFB/model/navigation suite 92/92;
  targeted ESLint passed; `npx tsc --noEmit` passed; Vite production build passed with only
  pre-existing warnings.
- Next step: set `CFBD_API_KEY`, run `npm run cfb:fetch-data`, then `npm run cfb:build-ratings` twice,
  review mapping/completeness and generated CSV hash, and only then wire reviewed output into UI.

## College Football Phase 2B authenticated fetch repair

- Diagnosed `/games/teams?year=2025&classification=fbs` HTTP 400: `classification` is accepted,
  but CFBD v5.20 requires one of `week`, `team`, or `conference` when `year` is supplied.
- Added credential-safe CFBD non-2xx diagnostics with path, status, safe query, and response body.
- Changed team game stats fetching to 17 season-type/week bulk requests derived from the 2025 games
  response; no per-team fan-out and no model/normalization calculation changes.
- Narrow test returned 96 week-1 rows with the expected game/team/stat shape and 35 categories.
- One resumed full fetch succeeded: teams 138, 2025 games 934, 2025 team stats 934, 2026 games
  888, returning production 136, talent 0; 975 CFBD calls remained.
- Cache audit: 934 unique stats game IDs, zero missing versus games cache, 808 FBS-vs-FBS and
  126 FBS-vs-FCS. Existing JKB FCS policy remains in normalization/model layers.
- Did not run `cfb:build-ratings`; no commit, push, merge, or deploy.

## College Football Phase 2B first model-generation blocker

- Added generator preflight validation for required cache files, expected rows, byte sizes, and
  SHA-256 hashes; all authenticated caches passed without a CFBD refetch.
- Corrected centralized verified CFBD names for FIU, NC State (`ncsu`, not nonexistent `ncst`),
  UConn, UL Monroe, and Massachusetts. Actual mapping is now 138/138 with no duplicates/unmapped.
- Corrected normalization to identify nine conference championships that CFBD labels as regular
  season and to exclude a mapped 2026 FBS team from opponent adjustment when it was FCS in 2025.
- Validated 2025 categories: 879 regular, 9 conference championships, 35 bowls, 11 CFP/playoff;
  808 FBS-vs-FBS and 126 FBS-vs-FCS.
- Stats validation: 934 game records, 1,743 mapped team rows, no duplicate game/team rows, no
  missing required derived stats, no non-finite/negative impossible rows.
- Returning production maps 136/136; missing North Dakota State and Sacramento State. Only
  offensive `percentPPA` is used; defense/QB remain null. Talent remains unavailable (0 rows).
- Opponent adjustment: 136 teams eligible; North Dakota State and Sacramento State fail the FBS
  prior-season minimum after symmetric FBS classification enforcement.
- First real `npm run cfb:build-ratings` stopped before writes: 137 computed ratings, with North
  Dakota State insufficient-data. NDSU appears only in `teams-2026`, not 2025 games/stats or 2026
  returning production; talent/QB/coaching are unavailable. No generated artifact/hash exists.
- Per instruction, did not weaken the 138-team gate, fabricate inputs, rerun, execute downstream
  determinism/full suites, switch UI, commit, push, merge, or deploy.

## College Football Phase 2B transition-team fallback and completed build

- Made exactly four additional CFBD calls (975 to 971 remaining): team-specific 2025 `/games` and
  `/games/teams` for North Dakota State and Sacramento State. No full cache was refetched.
- Dedicated transition cache SHA-256: `2deb4558e202c41218c68f95516ad7c4ac863b1da12e87b2f0eea6ab8712a7ad`.
  NDSU has 13 games/13 stats, all new; Sacramento State has 12/12, with one existing FBS-cache
  overlap (`401760365`) and 11 additional games.
- Added typed `prior-fcs-fallback` source metadata (classification, sample count, source IDs),
  shared normalization/deduplication, separate cache provenance, and no FCS opponent adjustment or
  cross-classification bonus/penalty. Returning/QB/talent remain null for both transition teams.
- NDSU raw prior: offense 7.2781345957, defense allowed 4.1694399129, 13 games. Sacramento State:
  offense 6.5016478947, defense allowed 5.4539236452, 12 games.
- The mandatory gate passed with 138 computed ratings and no insufficient-data teams.
- Two unchanged ratings builds produced identical CSV SHA-256
  `bfa74bef5b76ebf0fbf881555d4906769350d44750fe211c83f57e776ffcadc2`.
- Generated review artifacts under `data/generated/cfb/`; production UI still uses sample ratings.
- Full CFB/navigation suite: 20 files, 98 tests passed. Targeted lint: 0 errors, 2 existing Fast
  Refresh warnings. Vite build passed with existing warnings. `npx tsc --noEmit` passed.
- No commit, push, merge, deployment, or production data switch.
## CFB Phase 2C calibration
- Preserved `cfb-preseason-v0.1`; baseline CSV SHA-256 is `bfa74bef5b76ebf0fbf881555d4906769350d44750fe211c83f57e776ffcadc2`.
- Confirmed v0.1 raw-scale leakage: yards/play is combined directly with 0-100 returning percentPPA, so nominal weights do not represent practical contributions.
- Added an offline, leakage-safe 2025 calibration harness and separate `cfb-preseason-v0.2-candidate` config/artifacts; no CFBD calls and no production UI changes.
- Candidate uses league z-scores, 50/50 adjusted YPP + adjusted points/play per side, defense inverted to higher-is-better, 50/50 offense/defense power, opponent strength 0.20, 6 iterations, equal recency/postseason weights, and provisional standardized returning-production weight 10%.
- Fixed a Phase 2C-only audit bug before accepting results: defensive points/play uses opponent plays. Existing Phase 2B normalization/model was not changed.
- Walk-forward holdout: 411 regular-season FBS-vs-FBS games weeks 1-8 train; 341 weeks 9-14 test; test IDs excluded from training.
- Candidate CSV deterministic twice at `ef44714b03b372b934e0af9105c28b2f1b069446d280112bd5ef56749e9da432`.
- Official Pac-12 material confirms all eight 11-game CFBD schedules omit the intentionally unassigned Nov. 28 Week 13 flex matchup; SOS is provisional until assigned.
- Validation: 105 CFB/navigation tests passed; scoped lint passed; build passed; TypeScript passed. Full repo lint remains blocked by pre-existing unrelated errors (60 after the one new issue was fixed; full run originally reported 61 errors/108 warnings).
- Recommendation: candidate is a meaningful methodology correction but not production-ready because metric wins are mixed, returning production lacks outcome calibration, and FCS-to-FBS translation remains uncalibrated (NDSU ranks first on incomparable raw FCS data).
