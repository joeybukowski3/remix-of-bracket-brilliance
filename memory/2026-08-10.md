# 2026-08-10

- Completed the simplified College Football production model `cfb-preseason-v1` from the Phase 2C candidate architecture.
- Centralized a generic transition-team policy: any `prior-fcs-fallback` standardized prior is blended 50/50 with the FBS league-average standardized prior (zero), preserving provenance and applying no team-specific exceptions.
- Generated and validated 138 deterministic v1 ratings, complete ranks 1-138, Top 25, and preseason SOS from the cached 888-game 2026 schedule. The eight Pac-12 flex schedules retain provisional provenance.
- Generated `data/generated/cfb/2026-preseason-ratings-v1.csv`, matching JSON, and `2026-schedule-v1.json`. Two independent builds produced SHA-256 `9244ae905a4cbeeda33e8f1f1c019d1a74578bb65382a32e11dc4e8b14f59856`.
- Switched College Football data consumers and pages from sample preseason ratings/schedule to generated v1 data. Matchups remain comparison-only and show “Model projection coming soon”; no projected spread or pick was added.
- Validation passed: 23 Vitest files / 113 tests, targeted lint, `npm run build`, and `npx tsc --noEmit`. Build retains pre-existing Browserslist, unrelated duplicate NFL key, and chunk-size warnings.
- No CFBD requests, commits, pushes, merges, deployments, production betting projections, or subjective team overrides were made.
- Commit preparation follows repository conventions: raw authenticated CFBD caches and reproducible calibration/intermediate outputs are locally preserved but ignored; only the three v1 artifacts imported by runtime/tests are versioned.
- Final pre-commit validation passed again: 113/113 CFB/navigation tests, targeted lint with zero errors and two existing Fast Refresh warnings, production build, TypeScript, and staged `git diff --check`. Secret audit found no credential value or environment file in the intended commit.
- Updated the homepage College Football tile to use the local `public/images/leagues/ncaa.svg` asset in a 64x64 contained image box with `NCAA logo` alt text, while retaining one league label and the `Open` status. The two focused homepage test files passed (9/9), and the production build passed with existing warnings; no commit or push.
