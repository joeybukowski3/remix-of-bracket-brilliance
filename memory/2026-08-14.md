# 2026-08-14

- Added a permanent Playwright analytics safeguard shared by the Lovable fixture and every repository-owned standalone Playwright context/page creation path.
- Automated Playwright contexts now use `serviceWorkers: "block"` and abort requests to `google-analytics.com`, `*.google-analytics.com`, `googletagmanager.com`, and `*.googletagmanager.com` before navigation.
- Added a no-leak regression test and the required `AGENTS.md` instruction; production analytics code was not changed.
- Verification: analytics regression passed, affected social-card Vitest suite passed (11/11), focused ESLint and syntax/diff checks passed, and production build passed with pre-existing warnings. Full Vitest, full lint, and Playwright `--list` attempts timed out in the Windows sandbox after spawning many Node workers; the root Lovable Playwright package is also not locally declared/installed.
