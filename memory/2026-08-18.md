# 2026-08-18

## Numerology scoring + filters on `feat/numerology-update`

Isolated branch. Age removed from scoring (kept as profile display). Signal fields and signal types are Include/Exclude scoring controls, default Include.

Sin City is a standalone 0–100 grade and is not blended into Base.

## Player Explorer: two standalone ranking tabs

Replaced the mixed table + All Players | Has Sin City Match list filter with two views:

- Default tab: Sin City. Sort: Sin City Score desc, then Base Numerology, Model Rating, name A–Z.
- Numerology tab: Sort: Numerology Score desc, then Model Rating, name A–Z.

Each tab has its own collapsed Filter Settings menu. Shared search/team/match-type stay above the table. Scoring formulas were not changed; explorer now passes independent Sin City signal types so Numerology Include/Exclude no longer also drives Sin City.

Do not merge this branch.
