# 2026-08-20

- Implemented Phase 1 of the NFL Weekly Command Center foundation on `feat/nfl-weekly-command-center-v1`.
- Preserved the existing 32-team power board at `/nfl/power-ratings`; `/nfl` is now a minimal schedule-aware shell pending Phase 2 UI.
- Added `src/lib/nfl/weekSelection.ts` as the pure authority for regular-season week availability, explicit `?week=` selection, deterministic invalid-query fallback, and Eastern-time schedule-derived defaults.
- Migrated `/nfl/matchups` off its hard-coded Week 1 state to the shared resolver and URL-backed week selection.
- Fixed canonical matchup construction to propagate `game.neutralSite` and use `-vs-` slugs for neutral games while preserving normal `-at-` slugs.
- Focused tests and Vite build pass. Repository-wide `tsc` remains blocked by the pre-existing syntax error in `src/lib/mlb/mlbPitcherRegression.ts:28`; that unrelated file was not changed.
- Implemented Phase 2 of the NFL Weekly Command Center without committing, pushing, merging, or deploying.
- Added `src/lib/nfl/weeklyDashboard.ts` as a pure presentation derivation over canonical schedule, teams, market, projections, ratings, and weekly fantasy ranking rows; it owns no modeling formulas.
- Added `useNflWeeklyDashboard` orchestration and `useWeeklyFantasyRankings` so `/nfl` and the full fantasy rankings page consume the same QB/RB/WR/TE ranking output.
- Replaced the temporary `/nfl` shell with the responsive command center: complete chronological slate, descriptive Model-vs-Market gaps, fantasy leaders, Power Watch, and deep NFL navigation.
- Phase 2 validation passed: focused Vitest suites, independently rerun load-sensitive NFL regressions, changed-file ESLint, `git diff --check`, production Vite build, and analytics-blocked desktop/mobile Playwright checks. Mobile measured no core game-board horizontal overflow.
- Impeccable finish review approved the UI after removing an inadvertent `aria-hidden` from the desktop fantasy leaderboard.
