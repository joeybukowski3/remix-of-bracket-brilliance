import { cap } from "./NumerologyAuditCard";
const FIELDS=[["personalDay","Personal Day"],["jersey","Jersey"],["battingOrder","Batting Order"],["lifePath","Life Path"],["birthDay","Birth Day"],["age","Age"],["expression","Expression"],["repeatedDigit","Repeated Digit"]] as const;
const TYPES=[["exact","Exact"],["root","Root"],["family","Family Support"],["context","Contextual Echo"],["countercurrent","Countercurrent"]] as const;
const chip=(on:boolean)=>`rounded-full border px-2.5 py-1 text-[10px] ${on?"border-[#a078ff] bg-[#a078ff] text-[#26005d]":"border-[#494454] text-[#cbc3d7]"}`;
export function ExplorerFilters({query,setQuery,team,setTeam,teams,matchType,setMatchType,fields,setFields,types,setTypes}:{query:string;setQuery:(v:string)=>void;team:string;setTeam:(v:string)=>void;teams:string[];matchType:string;setMatchType:(v:string)=>void;fields:string[];setFields:(v:string[])=>void;types:string[];setTypes:(v:string[])=>void}){const toggle=(v:string,a:string[],set:(x:string[])=>void)=>set(a.includes(v)?a.filter(x=>x!==v):[...a,v]);return <div className="space-y-2 border-b border-[#494454] bg-[#191b24] px-4 py-3"><div className="flex flex-wrap gap-2"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search players" className="min-w-[180px] flex-1 rounded-lg border border-[#494454] bg-[#1d1f28] px-3 py-1.5 text-sm"/><select value={team} onChange={e=>setTeam(e.target.value)} className="rounded-lg border border-[#494454] bg-[#1d1f28] px-3 py-1.5 text-sm"><option value="all">All Teams</option>{teams.map(t=><option key={t}>{t}</option>)}</select><select value={matchType} onChange={e=>setMatchType(e.target.value)} className="rounded-lg border border-[#494454] bg-[#1d1f28] px-3 py-1.5 text-sm"><option value="all">All Match Types</option><option>Exact Match</option><option>Root Match</option></select></div><div><p className={`${cap} mb-1 text-[#958ea0]`}>Signal fields &#8212; matches any selected</p><div className="flex flex-wrap gap-1">{FIELDS.map(([v,t])=><button key={v} type="button" onClick={()=>toggle(v,fields,setFields)} className={chip(fields.includes(v))}>{t}</button>)}</div></div><div><p className={`${cap} mb-1 text-[#958ea0]`}>Signal types &#8212; matches any selected</p><div className="flex flex-wrap gap-1">{TYPES.map(([v,t])=><button key={v} type="button" onClick={()=>toggle(v,types,setTypes)} className={chip(types.includes(v))}>{t}</button>)}</div></div></div>}
